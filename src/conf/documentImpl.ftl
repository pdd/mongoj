package ${package}.model.impl;

import java.util.Map;
import ${package}.model.${document.name};

/**
 * Edit this file and re-run ServiceBuilder to propagate new method signatures to interfaces and utilities.
 */
public class ${document.name}Impl extends ${document.name}ModelImpl implements ${document.name} {
	public ${document.name}Impl() {
	}

	public ${document.name}Impl(Map<String, Object> map) {
		super(map);
	}

}