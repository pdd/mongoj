package ${package}.service.impl;

import ${package}.service.${document.name}LocalService;
import ${package}.service.base.${document.name}LocalServiceBaseImpl;

/**
 * Edit this file and re-run ServiceBuilder to propagate new method signatures to interfaces and utilities.
 */
public class ${document.name}LocalServiceImpl extends ${document.name}LocalServiceBaseImpl {
}